# Планировщик задач (TODO-лист)

Веб-сервер на Go с REST API и SQLite. Хранит задачи с датой, заголовком, комментарием и правилом повторения.

## Выполненные задания со звёздочкой
- Переменные окружения `TODO_PORT`, `TODO_DBFILE`
- Правила повторения `w` (по дням недели) и `m` (по дням месяца)
- Поиск задач по подстроке и дате (`/api/tasks?search=...`)
- Аутентификация через JWT (`TODO_PASSWORD`)
- Docker-образ

## Локальный запуск
```bash
go mod tidy
go run .